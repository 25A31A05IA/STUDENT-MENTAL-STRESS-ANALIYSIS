....TIME COMPLEXITY....
.The loop runs from 1 to 9 → fixed number of iterations
.Each iteration performs:
    .Display statements → O(1)
    .Input (cin) → O(1)
    .switch + if-else → O(1)
.No nested loops
.No dependence on input size (n)
.Total operations are constant
-->Final Time Complexity:
O(1) (Constant Time)

....SPACE COMPLEXITY....
.Variables used:
    .totalScore
    .choice
    .q (loop variable)
.No extra memory allocation:
    .No arrays
    .No recursion
    .No dynamic memory
.Memory usage stays constant regardless of input
--> Final Space Complexity:
O(1) (Constant Space)
